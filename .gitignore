# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ---- PowerPlan additions ----
# Secrets / environment (none needed today, but never commit them)
.env
.env.*
!.env.example

# Local-only operational notes (exact resource names, URLs, etc.) — never commit
NOTES.local.md

# Build / tooling caches
*.tsbuildinfo
.vite/
coverage/

# Azure CLI local state
.azure/

# Generated export artifacts (created when running the exporters in Node during tests)
/powerplan-comparison.pdf
/powerplan-comparison.xlsx

